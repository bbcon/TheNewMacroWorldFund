# Instrument Metadata

`etf_universe.yml` is the single source of truth for tradable tickers, sectors, and base currency. Add other files here (rates curves, futures contract codes, FX crosses) so scripts stay decoupled from hard-coded lists.
